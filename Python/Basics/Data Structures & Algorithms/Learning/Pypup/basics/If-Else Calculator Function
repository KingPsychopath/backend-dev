# Return Type = int
# Type of "first_int" = int
# Type of "second_int" = int
# Type of "oprt" = str
def calculator(first_int, second_int, oprt):
    if oprt == '+':
        return first_int + second_int
    elif oprt == '-':
        return first_int - second_int
    elif oprt == '/':
        return first_int / second_int
    else:
        return first_int * second_int
    
"""
Write a Python calculator function that accepts three parameters. The first and second parameters are integers. The last parameter is one of the following mathematical operators: (+), (-), (/), or (*). Although there are many mathematical operators, we will focus on the listed operators in this problem. Use the given operator to calculate the resultant value.

"""

# Better Solution

"""
op = {'+': lambda x, y: x + y,
      '-': lambda x, y: x - y}
Then you can do:

print(op['+'](1,2))
And it will output:

3
"""