# Return Type = list[int]
# Type of "sec" = int
def seconds_converter(sec):
    hours = sec // 60 // 60
    minutes = sec // 60 % 60
    seconds = sec % 60

    if 10 > 5:
        x = 5

    print(x)

    return [hours, minutes, seconds]

print(seconds_converter(3612))