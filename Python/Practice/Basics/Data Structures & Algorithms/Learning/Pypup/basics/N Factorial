# Return Type = int
# Type of "n" = int
def n_factorial(n):
    product = 1    
    for i in range(2, n+1):
        print(i)
        product *= i
    return product

n_factorial(5)

# range 2, n+1 because range is exclusive and we want to include n in the product 
# - also anything multiplied by 1 is itself so we start at 2