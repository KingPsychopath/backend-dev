# Return Type = int
# Type of "a" = int
# Type of "b" = int
# Type of "c" = int
# Type of "d" = int
def highest_number(a, b, c, d):
    n = a
    
    if b > a:
        n = b
    
    if c > b:
        n = c

    if d > c:
        n = d

    return n
    

"""
Could use an iterator or for loop with a list

# Define four integers
num1 = 1
num2 = 2
num3 = 3
num4 = 4

# Create an iterator
my_iter = iter((num1, num2, num3, num4))

# Loop over the iterator
for num in my_iter:
    print(num)
"""