def print_star_pattern(n):
    """
    Prints a star pattern with n rows, where the first row has 1 star and each subsequent row has one more star than the previous row.

    Args:
    n (int): The number of rows in the star pattern.

    Returns:
    None
    """
    for i in range(1, n + 1): # iterate through each row of the star pattern
        print(' ' * (n - i) + '*' * i) # print spaces before the stars to right justify the pattern, and print the stars for the current row

print_star_pattern(5)


