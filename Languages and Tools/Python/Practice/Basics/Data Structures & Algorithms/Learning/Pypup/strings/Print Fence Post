# Type of "n" = int
def fence_post_i(n):
    fence = "|=="

    for i in range(n):
        print(fence, end="")

def fence_post_i(n):
   print("|==" * n)

# You can print a string multiple times by multiplying it by a number
# as long as the string is in quotes and comes before the integer